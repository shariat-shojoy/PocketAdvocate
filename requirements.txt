# Keep the cloud deployment CPU-compatible. Railway does not provide an NVIDIA
# CUDA runtime; the app automatically uses CUDA locally when a CUDA build of
# PyTorch is installed.
torch
pandas
numpy
sentence-transformers
transformers
faiss-cpu
openai
python-dotenv
Pillow
tqdm
streamlit
